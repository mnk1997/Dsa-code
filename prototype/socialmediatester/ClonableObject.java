package in.cdac.prototype.socialmediatester;

/*interface named ObjectClonable with a single method, 
clone(), that returns a cloned copy of the implementing object.*/
public interface ClonableObject<T> {
    T cloneObject();
}
